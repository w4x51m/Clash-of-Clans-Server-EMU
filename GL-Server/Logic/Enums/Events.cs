namespace GL.Servers.CoC.Logic.Enums
{
    public enum Events
    {
        DEFAULT,
        KICK_CLAN,
        ACCEPT_MEMBER,
        JOIN_CLAN,
        LEAVE_CLAN,
        PROMOTE,
        DEMOTE,
        SEARCH_WAR_STARTED,
        SEARCH_WAR_CANCELED,
        SEARCH_WAR_ERROR,
        UPDATE_SETTINGS
    }
}